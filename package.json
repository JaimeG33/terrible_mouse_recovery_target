{
  "name": "terrible-mouse-recovery-target",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Manual-navigation capture harness for XHTML fragments already rendered by the McGraw Hill reader.",
  "scripts": {
    "inspect": "node src/inspect.mjs",
    "capture": "node src/capture.mjs",
    "validate": "node src/validate.mjs"
  },
  "dependencies": {
    "playwright-core": "^1.55.0"
  }
}
